name: Send submodule updates to parent repo

on:
  push:
    branches: 
      - main

jobs:
  update_dbweb:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with: 
          repository: emiyl/appledb-web
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull & update submodules recursively
        run: |
          git submodule update --init docs/.vuepress/public/assets/images/device
          git submodule update --remote docs/.vuepress/public/assets/images/device
      - name: Commit
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions - update submodules"
          git add --all
          git commit -m "Update apple-device-images submodule" || echo "No changes to commit"
          git push
